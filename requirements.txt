# TRINETRA — Python dependencies
# Python 3.11. Install with:  make install
#
# NOTE on torch: the CPU wheel is listed here for portability. For the RTX 2050
# (CUDA 12.1) install the GPU build FIRST, then this file:
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt

# ---------------------------------------------------------------- web / API
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20
httpx==0.28.1
python-dotenv==1.0.1

# ---------------------------------------------------------------- datastores
psycopg2-binary==2.9.10
# geopandas.to_postgis needs SQLAlchemy + GeoAlchemy2 for the geometry type.
SQLAlchemy==2.0.36
GeoAlchemy2==0.16.0
qdrant-client==1.12.2
duckdb==1.1.3
pyarrow==18.1.0

# ---------------------------------------------------------------- geospatial
# rasterio ships its own GDAL wheels on Windows — no system GDAL needed.
rasterio==1.4.3
shapely==2.0.6
pyproj==3.7.0
geopandas==1.0.1
rio-cogeo==5.4.1
pystac-client==0.8.5
planetary-computer==1.0.0
h3==4.2.0
mgrs==1.4.6
osmnx==2.0.1
scikit-image==0.25.0

# ---------------------------------------------------------------- ML / models
# torch + torchvision: see note above.
torch==2.5.1
torchvision==0.20.1
open-clip-torch==2.29.0
transformers==4.47.1
safetensors==0.4.5
huggingface-hub==0.27.0
timm==1.0.12
einops==0.8.0
ultralytics==8.3.55
numpy==1.26.4
scipy==1.14.1
# HDBSCAN comes from scikit-learn (>=1.3) as sklearn.cluster.HDBSCAN.
# The standalone `hdbscan` package needs a C++ toolchain on Windows and
# buys nothing here, so it is deliberately not a dependency.
scikit-learn==1.6.0

# ---------------------------------------------------------------- cloud mask
s2cloudless==1.7.2

# ---------------------------------------------------------------- LLM / VLM
groq==0.13.1

# ---------------------------------------------------------------- misc
PyYAML==6.0.2
Pillow==11.0.0
tqdm==4.67.1

# ---------------------------------------------------------------- test
pytest==8.3.4
pytest-asyncio==0.25.0
